---
import Layout from '../components/Layout.astro';
import FeedbackForm from '../components/FeedbackForm.astro';

const responsePorto = await fetch(
   "https://www.esky.pt/api/v1.0/deals.json?partner_id=AFFISEEU&dci=OPO&Filter[0][Type]=DepartureCity&Filter[0][Code]=OPO&f[0][t]=dci&f[0][c]=OPO&l=5"
);


const porto = await responsePorto.json();
---

<Layout title="Astro Toolbox">
  <main>
<Fragment set:html={responsePorto.deals} />
teste
 {
        porto.map((item) => (
            <a href={item.deals.url}>{item.deals.url}</a>
        ))
        }
<Fragment set:html={responsePorto.deals} />


  </main>
</Layout>

<style>
  .logo img {
    padding: 30px 0;
    width: 100%;
  }

  main {
    padding: 2rem 20px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    margin: 0 auto;
    max-width: 60ch;
    align-items: center;
  }

  footer {
    width: 100%;
    height: 100px;
    border-top: 1px solid #eaeaea;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  footer img {
    width: 20px;
  }
</style>
